export default {
    root: {
        class: 'relative'
    },
    menu: {
        class: 'p-0 m-0 list-none flex'
    },
    menuitem: {
        class: [
            // Flexbox and Position
            'relative',
            'flex',
        ]
        
    },
    action: ({ props }) => ({
        class: [
            // Flexbox

            // Transitions and Shape

            // Colors

            // States

            // Misc

        ]
    }),
    step: ({ context, props }) => ({
        class: []
    }),
    label: ({ context }) => ({
        class: [
            // Font

            // Display

            // Spacing

            // Colors

            // Text and Overflow
        ]
    })
};